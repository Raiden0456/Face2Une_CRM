{
    "name": "face2une_crm",
    "description": "a really cool face2une CRM",
    "version": "1.0.0",
    "engines": {
        "node": "18.x"
    },
"scripts": {
    "build": "cd backend && npm run build && cd .. && cd frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}